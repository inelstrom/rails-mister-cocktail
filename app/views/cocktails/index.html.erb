<div class="container mt-3">

  <div class="row">
    <div class="col-12">
      <h1>Cocktails</h1>
    </div>
  </div>

    <div class="row">
      <% @cocktails.each do |c| %>
        <div class="card">
          <%= c.name %>
          <%= link_to "Details", cocktail_path(c[:id]), class: "btn btn-outline-primary"%>
        </div>
      <% end %>
    </div>


</div>
